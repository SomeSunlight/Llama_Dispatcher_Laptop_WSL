# RTX500 Vulkan engine for the Laptop instance.

environment:
  # Keep GPU selection with the engine so callers never need to remember it.
  GGML_VK_VISIBLE_DEVICES: "1"

common:
  n-gpu-layers: 99
  split-mode: none
  main-gpu: 0
  flash-attn: on

serve:
  no-kv-offload: true
  cache-ram: 0

# NVIDIA RTX 500 Ada Vulkan engine for the Laptop_WSL instance.
# AI Workstation supplies the concrete llama.cpp binary directory at runtime.

environment:
  GGML_VK_VISIBLE_DEVICES: "1"

common:
  split-mode: none
  main-gpu: 0

serve:
  no-kv-offload: true
  cache-ram: 0

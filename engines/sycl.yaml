# Intel SYCL engine for the Laptop instance.
# bin_dir remains a Windows/manual fallback; managed launchers override it with --bin-dir.
bin_dir: 'c:\llama.cpp\server\server_07_SYCL'

environment:
  # Prefer the Level Zero GPU deterministically instead of relying on caller shell state.
  ONEAPI_DEVICE_SELECTOR: "level_zero:0"
